export const FRAME_PATHS = Array.from({length: 300}, (_, i) => `ezgif-frame-${String(i+1).padStart(3, '0')}.jpg`);
export const MAPPED_FRAME_PATHS = Array.from({length: 300}, (_, i) => `ezgif-frame-${String(i+1).padStart(3, '0')}.jpg`);
